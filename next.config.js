/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost','previews.123rf.com', 'yt3.ggpht.com', 'images.unsplash.com'],
  },
}

module.exports = nextConfig
